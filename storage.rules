
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Regra para a pasta de usuários:
    // Garante que um usuário só possa ler e escrever em sua própria pasta.
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
